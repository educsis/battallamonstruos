# Batalla de Monstruos — Vue 3 + Vite
CRUD de monstruos + batallas por turnos (reglas: orden por velocidad/ataque, daño = atk - def mínimo 1, fin cuando HP llega a 0).
## Scripts
npm install
npm run dev
npm run build
npm run preview
